# emotion-model
emotion detection using BERT and NLP
# Emotion Detection System

This project is an AI-based application that detects emotions from text using Natural Language Processing (NLP) and the BERT model.

## Features
- Detects emotions from user input text
- Uses pretrained BERT model
- Provides prediction with confidence score
- Interactive web interface using Streamlit

## Technologies Used
- Python
- PyTorch
- Hugging Face Transformers
- Streamlit
- Pandas

## How to Run
1. Install dependencies:
   pip install -r requirements.txt

2. Run the application:
   streamlit run app.py

## Project Structure
- app.py → Streamlit UI
- predict.py → Model prediction logic
- requirements.txt → Dependencies
